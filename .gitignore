node_modules/
dist/
coverage/
*.log

# skill-registry cache — regenerated by the skill-registry skill
.atl/

# local env — never commit
.env.local
.env*.local

# Local Codex hooks may contain workstation-specific paths.
.codex/
